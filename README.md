# Netork-Deep-Inspection
# Install dependencies pip install scapy flask flask-socketio  # Run with auto-detect sudo python forensics_tool.py  # Analyze offline PCAP python forensics_tool.py -f capture.pcap
